{
  "name": "temporal-polyfill-cf-bug-repro",
  "version": "1.0.0",
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "description": "Minimal reproduction for temporal-polyfill new Date() module scope bug in Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "temporal-polyfill": "^0.3.0"
  }
}
